# Decisions

Decisions are topic-keyed current governance documents. Use one Markdown file
per topic, for example `auth-strategy.md`.

Decision frontmatter must include `id`, `status`, `governs`, `evidence`, and
`last_verified` when the evidence requires inspection.
