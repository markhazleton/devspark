id: work-packages
name: Work Packages
kind: ephemeral-state
summary: v4 specification packages retained under `.devspark.work` through implementation and verification until release validates and archives them.
owner: DevSpark maintainers
lifecycle: current
root: .devspark.work
managed_by: prompt
required_layers:
- architecture.md
relations:
  - type: validated_by
    object: current-truth-ontology
evidence:
  - type: schema
    ref: templates/schemas/devspark-work-package.schema.json
    verified_by: inspection
