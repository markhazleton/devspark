# Pull Request Review: refactor: split harness/runner.py into focused modules (QUAL2)

```yaml
gate: pr-review
status: warn
blocking: true
severity: warning
summary: "High-risk backward-compatibility regression detected: several callable HarnessRunner methods were removed from runner.py without compatibility delegates."
```

## Review Metadata

- **PR Number**: #34
- **Source Branch**: copilot/qual2-refactor-harness-runner
- **Target Branch**: main
- **Review Date**: 2026-05-05 19:26:13 UTC
- **Last Updated**: 2026-05-05 19:26:13 UTC
- **Reviewed Commit**: fd0d1ee4d82fdbee5e6e933e5ce819a2fd138335
- **Reviewer**: devspark.pr-review
- **Constitution Version**: 1.3.0

## Revision Log

| Rev | Commit | Date | Critical | High | Medium | Low | CON | Test Command | Result |
|-----|--------|------|----------|------|--------|-----|-----|--------------|--------|
| 1 | fd0d1ee4d82f | 2026-05-05 | 0 | 1 | 0 | 0 | 0 | `..\\..\\.venv\\Scripts\\python.exe -m pytest -q` (full suite fallback; no changed test files) | pass (123 passed, 1 skipped) |

*Add a row for each re-review. Keep the same test command across all revisions to prevent flaky baselines.*

## PR Summary

- **Author**: @app/copilot-swe-agent
- **Created**: 2026-05-05T16:36:24Z
- **Status**: OPEN
- **Files Changed**: 5
- **Commits**: 3
- **Lines**: +1160 -1027

## Stats

| Metric | Value |
|--------|-------|
| Files changed | 5 |
| Lines added | +1160 |
| Lines removed | -1027 |
| Net lines | +133 |
| Commit snapshot | `fd0d1ee4d82f` |

*Collected via `git diff --numstat`. Re-collect on every revision for trend tracking.*

## Executive Summary

- ✅ **Constitution Compliance**: FAIL (7/8 principles checked)
- 📋 **Spec Lifecycle**: N/A (not a feature branch)
- 📝 **Task Completion**: N/A
- 🔒 **Security**: 0 issues found
- 📊 **Code Quality**: 1 recommendations
- 🧪 **Testing**: PASS
- 📝 **Documentation**: N/A
- 🏛️ **Constitution Improvements**: 0 CON findings

**Overall Assessment**: The modular split improves maintainability, and tests pass on PR head. However, the refactor appears to remove several previously callable `HarnessRunner` methods from `runner.py` with no class-level compatibility delegates, creating a silent API-surface contraction risk.

**Approval Recommendation**: ⚠️ REQUEST CHANGES
*Note: APPROVE is blocked if Spec Lifecycle is not Complete or tasks are incomplete for feature branches.*

## Action Items

*All findings ordered by severity. CRITICAL and HIGH items include broken code and the fix.*

### Immediate Actions (Blocking — must resolve before merge)

- [ ] **H-01** `src/devspark_cli/harness/runner.py:23` — Preserve class-level compatibility wrappers for removed callable methods to avoid backward-compatibility regressions.
  - **Broken code**: `class HarnessRunner` on PR head no longer defines methods that existed on `main` (`resolve_step_adapter`, `get_adapter`, `write_supporting_artifacts`, `write_result`, `write_lifecycle_artifacts`, `build_retry_prompt`, `pause_for_human_review`).
  - **Fix**: Re-add thin compatibility delegates on `HarnessRunner` that forward to the new module functions (for example in `_probe_dispatch.py` and `_result_fmt.py`), or provide explicit deprecation shims with release-note documentation before removing these methods.

### Recommended Improvements

None found.

### Constitution Improvements (Non-blocking — feed into `/devspark.evolve-constitution`)

None found.

## What's Good

- Branch-sync hard gate passed (`is_behind_target: false`) before review execution.
- The refactor cleanly decomposes responsibilities into focused modules (`_convergence.py`, `_delivery_gate.py`, `_probe_dispatch.py`, `_result_fmt.py`).
- The max-pass report bug fix included in this PR (`pass_count` handling) is a concrete correctness improvement.
- Mandatory test validation passed on PR head (`123 passed, 1 skipped`).

## Findings Detail

*Stable IDs persist across re-reviews. Status updates instead of deleting.*

### Critical Issues (Blocking)

None found.

### High Priority Issues

| ID | Status | Principle | File:Line | Issue | Fix |
|----|--------|-----------|-----------|-------|-----|
| H-01 | 🔴 Open | I. Backward Compatibility (NON-NEGOTIABLE) | src/devspark_cli/harness/runner.py:23 | Constitution language: "Existing single-application repositories must continue to work without restructure or behavior changes." Main branch defines callable `HarnessRunner` methods at lines 266, 272, 313, 325, 333, 965, 976 that are absent on PR head. This is a silent behavioral/API change for callers that invoke those methods directly. | Restore class-level method compatibility via thin delegates (preferred) or add explicit, documented deprecation shims and staged removal. |

### Medium Priority Suggestions

None found.

### Low Priority Improvements

None found.

### Constitution Improvements

*Findings where the code may be correct but the constitution needs updating. Feed these into `/devspark.evolve-constitution`.*

None found.

## Constitution Alignment Details

| Principle | Status | Evidence | Notes |
|-----------|--------|----------|-------|
| I. Backward Compatibility (NON-NEGOTIABLE) | ❌ Fail | `src/devspark_cli/harness/runner.py:23` | Callable methods present on main were removed from class surface in head branch without compatibility wrappers. |
| II. Explicit Over Implied (NON-NEGOTIABLE) | ✅ Pass | `src/devspark_cli/harness/runner.py:105` | Delegation wiring between orchestrator and split modules is explicit. |
| III. Ownership Boundary (NON-NEGOTIABLE) | ✅ Pass | All changed files are under `src/devspark_cli/harness/` | No `.documentation/` mutation behavior added. |
| IV. Governance Authority | ✅ Pass | Constitution-reviewed output generated and persisted | Review criteria follow repository constitution. |
| V. Simplicity | ✅ Pass | `src/devspark_cli/harness/_*.py` split by concern | Monolith decomposition reduces complexity in `runner.py`. |
| VI. Platform Parity | ✅ Pass | No bash/powershell script changes in PR | No parity drift introduced. |
| VII. PR Review Artifact Commit Discipline (MUST) | ✅ Pass | PR diff excludes `.documentation/specs/pr-review/pr-34.md` | No co-mingling detected in this PR. |
| VIII. Markdown Quality (MUST) | ⏭️ N/A | No markdown files changed in PR | No markdown lint risk introduced by this diff. |

## Security Checklist

- [x] No hardcoded secrets or credentials
- [x] Input validation present where needed
- [x] Authentication/authorization checks appropriate
- [x] No SQL injection vulnerabilities
- [x] No XSS vulnerabilities
- [x] Dependencies reviewed for vulnerabilities

No auth, SQL, web rendering, or dependency-surface changes were introduced in this PR.

## Testing Coverage

**Status**: ADEQUATE

No test files were changed in this PR, so scoped execution was not possible; mandatory fallback full-suite execution on PR head passed.

## Test Inventory

No test files changed in this PR.

## Documentation Status

**Status**: N/A

No constitution-triggered documentation updates were required for this internal refactor.

## Changed Files Summary

| File | Tier | Changes | Type | Findings |
|------|------|---------|------|---------|
| src/devspark_cli/harness/runner.py | P1 | +50 -1027 | Modified | H-01 |
| src/devspark_cli/harness/_probe_dispatch.py | P1 | +496 -0 | Added | None |
| src/devspark_cli/harness/_result_fmt.py | P1 | +327 -0 | Added | None |
| src/devspark_cli/harness/_convergence.py | P1 | +174 -0 | Added | None |
| src/devspark_cli/harness/_delivery_gate.py | P1 | +113 -0 | Added | None |

## Behavioral Changes

| Change | Before | After | Intentional? | Risk |
|--------|--------|-------|-------------|------|
| `HarnessRunner` callable helper methods | Methods existed on class surface (`resolve_step_adapter`, `get_adapter`, `write_supporting_artifacts`, `write_result`, `write_lifecycle_artifacts`, `build_retry_prompt`, `pause_for_human_review`) | Methods are absent from class on PR head | Unclear (PR body claims "full public interface" retained) | Downstream callers can fail at runtime with `AttributeError`; behavior changes without type-level warning for dynamic usage |

## Approval Decision

**Recommendation**: ⚠️ REQUEST CHANGES

**Reasoning**:
While the refactor quality is generally strong and tests pass, the removal of previously callable `HarnessRunner` methods introduces a backward-compatibility risk against Constitution Principle I. A compatibility layer or explicit staged deprecation is needed before merge.

**Estimated Rework Time**: 2-4 hours

---

*Review generated by devspark.pr-review v1.2*
*Constitution-driven code review for DevSpark*
*To re-review after fixes: `/devspark.pr-review #34 re-review`*
*When addressing these findings, run `/devspark.address-pr-review 34`. The review file must be committed on its own — this rule is enforced by the prompt and can also be enforced by the optional pre-commit hook.*

---

## Previous Review History

N/A - First review for PR #34.

## Shared Review Resolution Contract Output

```yaml
findings:
  - finding_id: H-01
    severity: high
    description: HarnessRunner class surface was contracted by removing previously callable methods without compatibility delegates, creating backward-compatibility risk.
    recommended_action: Re-introduce thin class-level compatibility delegates (or explicit deprecation shims) for removed callable methods before merge.
    execution_mode: selective
    status: open
    outcome: ""
```